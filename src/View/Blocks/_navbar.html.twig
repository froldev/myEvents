<nav class="navbar navbar-expand-lg">
    <div class="menu-burger">
        <button class="navbar-toggler fixed-bottom" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mx-auto">
            {% for nav in navs %}
            <li class="nav-item">
                <a class="nav-link" href="{{ nav.link }}">{{ nav.text }}</a>
            </li>
            {% endfor %}
            <li class="nav-item">
                <a class="nav-link" href="/admin/login">Connexion</a>
            </li>
        </ul>
        </form>
    </div>
</nav>
